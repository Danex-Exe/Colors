Python library for working with color output in the terminal (ANSI, RGB, gradients, and styles) with a convenient CLI interface and a text encoding/decoding utility for compact data storage and transmission.

# Installation
```bash
pip install git+https://github.com/Danex-Exe/Colors.git
```