Библиотека Python для работы с цветовым выводом в терминале (ANSI, RGB, градиенты и стили) с удобным интерфейсом командной строки и утилитой кодирования/декодирования текста для компактного хранения и передачи данных.

# Установка
```bash
pip install git+https://github.com/Danex-Exe/Colors.git
```